import 'package:flutter/material.dart';

import 'colors.dart';

class AppTextStyles {
  AppTextStyles._();

  static const pageText = TextStyle(fontSize: 35, color: AppColors.pageText, fontFamily: 'MatthanSans', letterSpacing: 2,);
  static const header = TextStyle(fontSize: 72, color: AppColors.pageText, fontFamily: 'BebasNeue');
}